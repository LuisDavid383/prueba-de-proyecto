<Window x:Class="CapaPresentacion.wpfVerPartidos"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CapaPresentacion"
        mc:Ignorable="d"
        Title="wpfVerPartidos" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Título -->
            <RowDefinition Height="*"/>
            <!-- Tabla -->
        </Grid.RowDefinitions>

        <!-- TÍTULO DINÁMICO -->
        <TextBlock x:Name="txtTituloTorneo"
                   Grid.Row="0"
                   Text="GESTIÓN DEL TORNEO: [Nombre del Torneo]"
                   FontSize="26"
                   FontWeight="Bold"
                   Foreground="#FF9C27B0"
                   Margin="0 0 0 20"
                   HorizontalAlignment="Left"/>

        <!-- TABLA DE ENFRENTAMIENTOS -->
        <DataGrid x:Name="dgEnfrentamientos"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserResizeColumns="False"
                  AlternatingRowBackground="#FFF3E5F5"
                  HeadersVisibility="Column"
                  GridLinesVisibility="None"
                  FontSize="14"
                  RowHeight="32">

            <DataGrid.Columns>

                <DataGridTextColumn Header="Equipo Local"
                                    Binding="{Binding LocalNombre}"
                                    Width="2*"/>

                <DataGridTextColumn Header="Equipo Visitante"
                                    Binding="{Binding VisitanteNombre}"
                                    Width="2*"/>

                <DataGridTextColumn Header="Fecha/Hora"
                                    Binding="{Binding FechaPartido, StringFormat='dd/MM/yyyy HH:mm'}"
                                    Width="1.5*"/>

            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Window>
