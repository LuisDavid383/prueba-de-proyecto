<Window x:Class="CapaPresentacion.wpfModificarEquipo"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CapaPresentacion"
        mc:Ignorable="d"
        Title="wpfModificarEquipo" Height="480" Width="550"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="MODIFICAR EQUIPO" 
               FontSize="24" FontWeight="Bold" 
               HorizontalAlignment="Center" Margin="0,0,0,25"
               Foreground="#FF1E88E5"/>

        <StackPanel Grid.Row="1">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Nombre del Equipo: *" 
                       VerticalAlignment="Center" Margin="0,15,10,5" FontWeight="SemiBold"/>
                <TextBox x:Name="txtNombre" Grid.Row="1" Grid.Column="1" 
                     Margin="10,5" Padding="7" MaxLength="250"/>

            </Grid>

            <StackPanel Margin="0,20,0,20">
                <TextBlock Text="Descripción:" Margin="0,0,0,5" FontWeight="SemiBold"/>
                <TextBox x:Name="txtDescripcion" 
                     Height="162" 
                     TextWrapping="Wrap" 
                     AcceptsReturn="True" 
                     VerticalScrollBarVisibility="Auto" 
                     Padding="5"/>
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,30,0,0">
            <Button Content="💾 Guardar Cambios" 
                x:Name="btnGuardarCambios"
                Click="btnGuardarCambios_Click"
                Margin="0,0,15,0" Padding="15,10" 
                Background="#FF2196F3" Foreground="White" FontWeight="Bold"/>

            <Button Content="❌ Cancelar" 
                x:Name="btnCancelar"
                Click="btnCancelar_Click"
                Padding="15,10" 
                Background="#FFCCCCCC" Foreground="Black"/>
        </StackPanel>
    </Grid>
</Window>
